#pragma once

#include <vector>

std::vector<int> slicing(std::vector<int>, unsigned, unsigned);
int MaxSubarraySum(std::vector<int>);
int KadaneMSS(std::vector<int>);
void printVector(std::vector<int>);
